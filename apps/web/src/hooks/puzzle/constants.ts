/** Minimum pointer travel (px) before a touch drag is treated as a drag rather than a tap. */
export const TOUCH_DRAG_THRESHOLD_PX = 6;

/**
 * Snap distance as a fraction of the smaller piece dimension: a loose piece
 * within this radius of its target (or a neighbour) locks into place on release.
 */
export const SNAP_THRESHOLD_RATIO = 0.22;
